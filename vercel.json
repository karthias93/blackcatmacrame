{
  "version": 2,
  "routes": [
    { "src": "/backend", "dest": "/backend/server.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "buildCommand": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
}